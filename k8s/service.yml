apiVersion: v1
kind: Service
metadata:
  name: pdhtm
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "pdhtm"
    port: 8080
  selector:
    app: pdhtm
