apiVersion: v1
kind: Service
metadata:
  name: rsehkg
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "rsehkg"
    port: 8080
  selector:
    app: rsehkg
