apiVersion: v1
kind: Service
metadata:
  name: gskhdh
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "gskhdh"
    port: 8080
  selector:
    app: gskhdh
